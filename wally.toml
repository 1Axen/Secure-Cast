[package]
name = "1axen/secure-cast"
version = "1.1.3"
description = "A server-authoritative projectile system for ROBLOX."
license = "MIT"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
exclude = ["**"]
include = ["src", "src/*", "wally.toml", "default.project.json", "LICENSE"]

[dependencies]
